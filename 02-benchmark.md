# 二、Benchmark 与 Bad Case 库（L1 数据资产层）

> 对应页面：`02-benchmark.html`
> 对应职责：设计并维护覆盖核心用户场景的 benchmark 和 bad case 库
> 核心思路：**分层构建 + 线上回流 + 持续保鲜**

建设原则：**评测集分布必须贴近真实流量分布**。一份离真实场景越来越远的评测集，分数再高也没有业务意义。通用学术 benchmark 只做参考基线，业务 benchmark 才是决策依据。

---

## 1. 分层 Benchmark 体系

| 层级 | 名称 | 定位 | 占比 |
|---|---|---|---|
| L1 | 通用基线集 | 引用学界公开 benchmark（MMLU、GSM8K、HumanEval 等）+ 内部通用能力集，用于模型选型与横向对比 | ~20% |
| L2 | 核心场景集 | 覆盖 Top 核心用户场景（按流量 / 价值排序），每场景独立成集、独立看分，**上线决策的主要依据** | ~50% |
| L3 | 对抗与边界集 | 越狱、注入、超长上下文、多轮诱导、语义陷阱、不可答问题（拒答测试）等边界能力 | ~15% |
| L4 | 回归 Bad Case 集 | 线上真实问题回流沉淀，每条「来路可查」，保证已修复问题不复发 | ~15%（持续增长） |

### 评测集管理台账（示例）

| 评测集 | 场景 | 规模 | 难度分布 | 更新频率 |
|---|---|---|---|---|
| core_qa_v12 | 核心问答 / RAG | 3,200 条 | 易 40% / 中 40% / 难 20% | 双周 |
| agent_task_v8 | Agent 多步任务 | 850 条 | 按步数 2~10 步分层 | 双周 |
| multimodal_doc_v4 | 多模态文档理解 | 1,100 条 | 按文档类型分层 | 月度 |
| safety_redline_v9 | 安全红线 | 1,500 条 | 按风险等级分层 | 周度 |
| regression_badcase_all | 历史问题回归 | 2,600+ 条（增长中） | 按问题类型分层 | 持续回流 |

## 2. 评测集数据来源（四条生产线）

| 生产线 | 内容 |
|---|---|
| **A 真实日志线** | 线上采样 → 清洗脱敏（去 PII）→ 按「场景 × 难度 × 用户类型」分层抽样，保留原始上下文，最贴近真实分布 |
| **B 人工构造线** | 依据产品场景规格（spec）编写，覆盖设计中的长尾分支；双人评审保证题面无歧义 |
| **C 模型合成线** | 用强模型对种子样本做改写 / 扩增（同义改写、难度升维、干扰注入），**必须经人工质检过滤后才能入库** |
| **D 问题回流线** | 来自 bad case 库与线上告警，是回归防线的弹药来源 |

## 3. Bad Case 库：收集 → 分类 → 复现 → 入库 → 回归

1. **收集**：五个来源——自动化评测失败样本、人工标注问题、用户负反馈（点踩/投诉）、客服工单、竞品对比落差
2. **去重与归并**：按问题指纹（场景 + 问题类型 + 触发模式）归并，避免同一 pattern 的重复 case 稀释回归权重
3. **复现与最小化**：确认在当前版本可复现，剥离无关信息形成最小复现单元（最小 prompt + 最小上下文）
4. **分类打标**：标注问题类型（幻觉 / 上下文遗忘 / 角色漂移 / 工具调用失败 / 安全风险 / 格式错误…）、严重等级（S1 致命 / S2 严重 / S3 一般）、影响场景
5. **入库登记**：写入 bad case 库，关联来源单号、版本号、复现脚本、期望行为、修复 Owner
6. **回归闭环**：自动汇入 L4 回归集，在每个候选版本上重跑；修复确认后转入「哨兵观察」状态持续监控

### Bad Case 记录结构（JSON）

```json
{
  "case_id": "BC-2026-0812-0034",
  "source": "user_feedback",
  "scene": "core_qa",
  "error_type": "hallucination",
  "severity": "S1",
  "repro_version": "app-2.4.1",
  "input": {},
  "expected": {},
  "status": "regression"
}
```

### 参考水位

| 指标 | 水位 |
|---|---|
| Bad Case 修复后回归拦截率 | 92% |
| S1 级 Case 入库到回归集时效 | 48h |
| 累计沉淀回归 Case 总量 | 2,600+ |
| 每周回流增量占回归集比例 | 15% |

## 4. 数据保鲜与治理机制

| 机制 | 内容 | 频率 |
|---|---|---|
| **防泄漏筛查** | 新模型发布后对评测集做污染检测（n-gram + 语义相似度），命中的题目降权或替换 | 每次接入新模型 |
| **分布漂移监控** | 对比线上流量分布与评测集分布（场景占比 / 长度 / 难度），偏差超阈值触发补采 | 月度 |
| **样本轮换** | 每季度替换 10~15% 低区分度样本（所有模型都得满分的「白给题」） | 季度 |
| **版本与快照** | 评测集统一版本号管理，历史评测结果绑定数据集快照，保证结果可复现、可追溯 | 持续 |
