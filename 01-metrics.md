# 一、评测指标体系（L0 标准指标层）

> 对应页面：`01-metrics.html`
> 对应职责：建立 LLM / Agent / 多模态应用的评测指标
> 核心原则：**把产品体验问题抽象成可测指标，而不是只做主观评价**

设计红线：每个指标必须有**明确定义、计算公式、数据来源、评测方式、目标值**。无法度量的指标不进入体系；只有主观判断的场景必须先通过标注校准转化为可测项。

---

## 1. 四层指标树

### 1.1 能力指标（效果 Quality）

回答正确性（Accuracy）、指令遵循率、幻觉率（Hallucination Rate）、引用可信度 / 事实一致性；Agent 专属：任务完成率（Success Rate）、工具调用成功率、规划合理性；多模态专属：图文一致性、OCR / 检测准确率。

### 1.2 体验指标（Experience）

相关性（Relevance）、完整性 / 结构化程度、语言自然度 / 风格一致性、上下文保持率（多轮遗忘）、角色一致性（角色漂移率）、首 Token 延迟 TTFT / 端到端延迟。

### 1.3 安全指标（Safety）

敏感内容拦截率、越狱攻击防御成功率、Prompt 注入防御率、隐私泄露率（PII）、价值观对齐合规率。**安全红线为一票否决项。**

### 1.4 成本指标（Cost & Stability）

单次请求 Token 成本、单任务平均成本、缓存命中率、服务可用性 / 错误率、输出稳定性（同题多次方差）。

## 2. 核心指标定义卡（示例）

每个指标在指标字典中以「定义卡」形式登记，保证跨团队口径一致：

| 指标 | 计算口径 | 评测方式 | 优先级 | 参考目标值 |
|---|---|---|---|---|
| 幻觉率 | 存在虚构事实 / 与给定资料矛盾的回答数 ÷ 总回答数 | LLM-as-Judge + 人工抽检校准 | P0 | 核心场景 ≤ 2% |
| 工具调用成功率 | 参数合法且执行成功的调用次数 ÷ 工具调用总次数 | 轨迹日志自动解析（规则评测器） | P0 | ≥ 98% |
| Agent 任务完成率 | 达到终态目标（含产物校验通过）的任务数 ÷ 总任务数 | 沙箱执行 + 结果断言 | P0 | ≥ 85%（按场景分档） |
| 上下文保持率 | 多轮对话中正确继承前文约束的轮次数 ÷ 应继承轮次数 | 多轮评测集 + LLM-as-Judge | P1 | ≥ 95% |
| 角色漂移率 | 偏离人设约束的回答轮次 ÷ 总轮次 | Judge 评分 + 人工仲裁 | P1 | ≤ 1% |
| 首 Token 延迟 | P50 / P90 / P99 分位的首包时间（ms） | 网关埋点自动采集 | P1 | P90 ≤ 2s |
| 图文一致性 | 生成 / 理解结果与图像内容相符的比例 | 多模态 Judge + 人工抽检 | P1 | ≥ 92% |
| 单任务成本 | Σ(Token 单价 × 用量) ÷ 任务数 | 调用日志自动汇总（SQL） | P2 | 环比不上升 |

## 3. 方法论：把「体验问题」抽象成可测指标（五步法）

1. **收集主观抱怨**：从用户反馈、客服工单、产品评审收集原始体验描述（如「回复太啰嗦」「答非所问」）
2. **聚类归并**：将抱怨聚类为可命名的质量维度（冗余度、相关性、结构清晰度等）
3. **给出可操作定义**：每个维度写出「什么算好、什么算差」的行为化描述与典型正反例
4. **设计量表**：转化为 1-5 分李克特量表或二元判定，配套锚定样例（anchor examples）
5. **校准验证**：多人标注 → 计算一致性（Kappa）→ 修订规范 → 达标后进入指标字典，可交由 Judge 自动化执行

### 指标字典登记示例（YAML）

```yaml
metric: hallucination_rate
name: 幻觉率
formula: hallucinated / total_answers
evaluator: llm_judge_v3    # 自动评测器
human_audit_ratio: 0.05    # 5% 人工抽检
dimension: quality
priority: P0
target: "<=0.02"
owner: eval-team
datasets: [core_qa_v12, rag_faq_v5]

# Judge 判定 Prompt 要点：
# 1) 只依据 <provided_context> 判断
# 2) 输出 JSON: {verdict, evidence_span}
# 3) 不确定时输出 uncertain，不计入分母
```

## 4. 分被测对象的指标矩阵

| 被测对象 | 必测指标组合 | 典型评测集 |
|---|---|---|
| **基础 LLM** | 正确性、幻觉率、指令遵循、上下文保持、安全性、成本/延迟 | 通用能力集 + 业务问答集 |
| **RAG 应用** | 检索召回率、引用可信度、忠实度（Faithfulness）、拒答正确率 | RAG 场景集（含不可答问题） |
| **Agent** | 任务完成率、工具调用成功率、规划步数效率、轨迹合规性、失败恢复率 | Agent 任务集 + 轨迹评测集 |
| **多模态应用** | 图文一致性、OCR 准确率、图表理解正确率、跨模态幻觉率 | 多模态场景集（图/表/文档） |
